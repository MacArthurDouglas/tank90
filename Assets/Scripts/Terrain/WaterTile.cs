using UnityEngine;

namespace Tank90
{
    /// <summary>Water blocks tanks (collider on the Wall layer) but bullets fly over it (see Bullet.HandleHit).</summary>
    public class WaterTile : MonoBehaviour { }
}
